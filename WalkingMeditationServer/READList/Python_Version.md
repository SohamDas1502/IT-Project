The current FASTAPI program needs python3.13


```cmd
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update
sudo apt install python3.13
```

We need to manually install ffmpeg also

```cmd
sudo apt install ffmpeg
```